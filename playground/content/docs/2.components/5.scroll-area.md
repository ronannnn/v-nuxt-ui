---
title: ProScrollArea
description: 自定义滚动区域组件，基于 reka-ui ScrollArea，支持边缘渐变效果、滚动检测、横向滚动提示和编程式滚动。
category: layout
---

## Examples

### Edge fade scrolling

::component-example{name="scroll-area-edge-fade"}
::

### Exposed Methods

| Method | Description |
| --- | --- |
| `scrollHorizontally(targetX)` | 水平滚动到绝对位置 |
| `scrollVertically(targetY)` | 垂直滚动到绝对位置 |
| `scrollHorizontallyBy(deltaX, behavior?)` | 水平增量滚动 |
| `scrollVerticallyBy(deltaY, behavior?)` | 垂直增量滚动 |

::tip
边缘渐变效果会自动根据滚动位置显示/隐藏，当内容滚动到顶部时，顶部渐变消失；滚动到底部时，底部渐变消失。
::

## API

### Props

:component-props{slug="scroll-area"}

### Slots

:component-slots{slug="scroll-area"}

### Emits

:component-emits{slug="scroll-area"}

## Theme

:component-theme{slug="scroll-area"}
