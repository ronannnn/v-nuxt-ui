---
title: ProPermissionWrapper
description: 权限控制包装器组件，通过注入的权限检查函数来控制内容的可见性或可交互性。支持 hide（隐藏）和 disable（禁用点击）两种模式。
category: layout
---

## Usage

### Mode

::component-code
---
slug: permission-wrapper
props:
  permission: admin:write
  mode: disable
slots:
  default: |
    <UButton>Create</UButton>
items:
  mode:
    - disable
    - hide
---
::

### Fallback

::component-code
---
slug: permission-wrapper
props:
  permission: admin:delete
  mode: hide
  fallback: true
slots:
  default: |
    <UButton color="error">Delete</UButton>
items:
  fallback:
    - true
    - false
---
::

## Examples

### Permission modes

::component-example{name="permission-wrapper-demo"}
::

::tip
宿主应用需要通过 `provide('vui:hasPermissions', fn)` 注入权限检查函数。如果未提供，默认允许所有权限。
::

## API

### Props

:component-props{slug="permission-wrapper"}

### Slots

:component-slots{slug="permission-wrapper"}

### Emits

:component-emits{slug="permission-wrapper"}

## Theme

:component-theme{slug="permission-wrapper"}
